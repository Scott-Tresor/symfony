{% extends 'base.html.twig' %}

{% block title %}Pinterest | Edition {{ pins.id }} {% endblock %}

{% block body %}
    <h1>Edited pins {{ pins.id }} </h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="Update" formnovalidate>
    {{ form_end(form) }}
{% endblock %}

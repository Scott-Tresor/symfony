{% extends 'base.html.twig' %}

{% block title %}Edith {% endblock %}

{% block body %}
    <div class="container mt-2">
        <h1>Edition</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}

            {# <input type="submit" value="{{ submit ?? 'Cree un article' }}" class="btn btn-primary btn-block" formnovalidate> #}
        {{ form_end(form) }}

        {# {{ include('layouts/forms/_form.thml.twig', {'submit': 'Edition'}) }} #}
    </div>
{% endblock %}